package ar.edu.utn.frba.dds.server;

import io.javalin.Javalin;

import java.util.Map;

public class App {
    /*
    public static void main(String[] args) {
        Javalin app = Javalin.create(config -> {

        }).start(7000);

        JavalinHandlebars.configure(h -> {
            h.setPrettyPrint(true);
        });

        app.get("/", ctx -> {
            // Renderizar una vista usando Handlebars
            ctx.render("/index.hbs", Map.of("mensaje", "Hola Mundo"));
        });

        // Otra ruta que usa Handlebars para renderizar una página
        //app.get("/donaciones", DonacionController::obtenerRecomendaciones);
    }
    */
}
